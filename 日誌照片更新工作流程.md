# 日誌與照片更新工作流程

目的：之後你傳訊息和照片給 Codex 時，只更新小檔案，避免反覆讀整份 HTML，節省 token，也降低誤改行程的機率。

## 你之後傳給我的格式

建議每次一則日誌用這個格式：

```text
日期：2026/7/5
標籤：明治神宮、澀谷、澀谷 Sky
標題：一句話標題
內容：今天實際怎麼走、吃了什麼、臨時改了什麼
花費：可省略；例如晚餐 ¥3,200、交通刷 Suica
照片：哪幾張要公開，哪些不要公開；每張可補一句 caption
注意：如果照片有 QR code、票券、訂位編號、護照或不想公開的人臉，請先說不要上傳
```

## 我收到後的固定處理

1. 先判斷內容是否適合公開網站，敏感資訊不放上去。
2. 把公開照片放到 `assets/journal/`，用日期和地點命名。
3. 把文字和照片路徑追加到 `data/journal.js`。
4. 檢查 `index.html` 和 `東京自由行10天行程與預算.html` 是否能正常顯示。
5. 更新部署資料夾，再視需要 commit、push、deploy 到 Netlify。

## 省 token 原則

- 不用重貼整份行程，只要說「更新 7/5 日誌」或「把這 3 張照片加到 7/6」。
- 如果要改既有日誌，只貼要改的那一句或那一段。
- 照片請直接傳原圖；我會只把要公開的版本放進 `assets/journal/`。
- 私人截圖、票券、QR code、訂位資料不要放公開站；若只是要我幫你整理，可以明確說「不要上傳」。

## 專案目前狀態摘要

- 首頁：`index.html`
- 同步備份頁：`東京自由行10天行程與預算.html`
- 行程文字來源：`東京自由行10天行程.md`
- PDF 離線版：`東京自由行10天行程與預算.pdf`
- 日誌資料檔：`data/journal.js`
- 公開照片資料夾：`assets/journal/`
- 對外網站：`https://tokyotrl.netlify.app`
- Git 狀態：本機 `main` 目前領先 `origin/main`，要公開程式碼更新需 push。

## 部署提醒

現在首頁會讀取 `data/journal.js`，照片會讀取 `assets/journal/`。以後部署時不能只放 `index.html`，需要一起部署：

- `index.html`
- `data/journal.js`
- `assets/journal/`